# ---------------------------------------------------------------------------- #
#This file has been auto-generated by i3-config-wizard(1).

# i3 config file (v4)
# dodotronix | CERN | 2019-09-11 Wed 20:45 PM 

# Please see https://i3wm.org/docs/userguide.html for a complete reference!
# NOTES:
#   * download nerd-fonts-complete 
# ---------------------------------------------------------------------------- #

#exec --no-startup-id nitrogen --restore
#exec --no-startup-id synergy
#exec --no-startup-id nm-applet
#exec --no-startup-id dropbox
#exec --no-startup-id gajim
#exec --no-startup-id xfce4-panel --disable-wm-check


#exec --no-startup-id evolution 
#exec --no-startup-id firefox

# COLORS
    # class                | border | background | text    | indicator | child_border |
    client.focused          #4d8341   #69ab55      #ffffff   #be302f     #69ab55
    client.focused_inactive #5f676a   #6c60a3      #ffffff   #6c60a3     #6c60a3
    client.unfocused        #4d8341   #426b57      #ffffff   #3e8b66     #7b7690 
    client.urgent           #e14c4c   #e14c4c      #ffffff   #e14c4c     #e14c4c
    client.placeholder      #00ff00   #00ff00      #ffffff   #000000     #0c0c0c
# END COLORS

# VARIABLES
#static workspaces variables
    set $ws_browser "0:a|🌐"
    set $ws_mail "1:o|📡"
    set $ws_music "2:e|🎧"
    set $ws_chat "3:u|💬"
    set $ws_todo "4:i|📝"
# end static workspaces variables

# GAPS
    smart_gaps on
    gaps inner 2
    gaps outer 2

# BORDERS
    # default_floating_border pixel 4
    # default_border pixel 4
    for_window [class=.*] border pixel 3
    title_align center

# MODES
## reorganize workspaces
    mode "organize" {
        #switch to workspace
        bindsym a workspace number 0; mode "default"
        bindsym o workspace number 1; mode "default"
        bindsym e workspace number 2; mode "default"
        bindsym u workspace number 3; mode "default"
        bindsym i workspace number 4; mode "default"
        bindsym d workspace number 5; mode "default"
        bindsym h workspace number 6; mode "default"
        bindsym t workspace number 7; mode "default"
        bindsym n workspace number 8; mode "default"
        bindsym s workspace number 9; mode "default"

        # move focused container to workspace
        bindsym ctrl+a move container to workspace number 0
        bindsym ctrl+o move container to workspace number 1
        bindsym ctrl+e move container to workspace number 2
        bindsym ctrl+u move container to workspace number 3
        bindsym ctrl+i move container to workspace number 4
        bindsym ctrl+d move container to workspace number 5
        bindsym ctrl+h move container to workspace number 6
        bindsym ctrl+t move container to workspace number 7
        bindsym ctrl+n move container to workspace number 8 
        bindsym ctrl+s move container to workspace number 9

        bindsym Shift+h move container to output left
        bindsym Shift+l move container to output right

        #return back to normal
        bindsym escape mode "default"
        bindsym space mode "default"
        bindsym $mod+u mode "default"
    }
    bindsym $mod+u mode "organize"
## end reorganize workspaces

## move workspace right
    mode "moveR"{
        bindsym a [workspace="0"] move workspace to output right;
        bindsym o [workspace="1"] move workspace to output right;
        bindsym e [workspace="2"] move workspace to output right;
        bindsym u [workspace="3"] move workspace to output right;
        bindsym i [workspace="4"] move workspace to output right;
        bindsym d [workspace="5"] move workspace to output right;
        bindsym h [workspace="6"] move workspace to output right;
        bindsym t [workspace="7"] move workspace to output right;
        bindsym n [workspace="8"] move workspace to output right;
        bindsym s [workspace="9"] move workspace to output right;
        
        bindsym space mode "default"
        bindsym $mod+Shift+T mode "default"
    }
    bindsym $mod+Shift+T mode "moveR"
## end move workspace right

## move workspace left
    mode "moveL"{
        bindsym a [workspace="0"] move workspace to output left;
        bindsym o [workspace="1"] move workspace to output left;
        bindsym e [workspace="2"] move workspace to output left;
        bindsym u [workspace="3"] move workspace to output left;
        bindsym i [workspace="4"] move workspace to output left;
        bindsym d [workspace="5"] move workspace to output left;
        bindsym h [workspace="6"] move workspace to output left;
        bindsym t [workspace="7"] move workspace to output left;
        bindsym n [workspace="8"] move workspace to output left;
        bindsym s [workspace="9"] move workspace to output left;

        bindsym space mode "default"
        bindsym $mod+Shift+D mode "default"
    }
    bindsym $mod+Shift+D mode "moveL"
## end move workspace right
#
## resize windows
    mode "resize" {
            bindsym l resize grow width 10 px or 10 ppt
            bindsym h resize shrink width 10 px or 10 ppt
            bindsym j resize shrink height 10 px or 10 ppt
            bindsym k resize grow height 10 px or 10 ppt

            # same bindings, but for the arrow keys
            bindsym Left resize shrink width 10 px or 10 ppt
            bindsym Down resize grow height 10 px or 10 ppt
            bindsym Up resize shrink height 10 px or 10 ppt
            bindsym Right resize grow width 10 px or 10 ppt

            # back to normal: Enter or Escape or $mod+r
            bindsym space mode "default"
            bindsym Escape mode "default"
            bindsym $mod+d mode "default"
    }
    bindsym $mod+d mode "resize"
## end resize windows

# STARTUP
## additional scripts 
    exec --no-startup-id $HOME/.config/i3/i3wsort.py
    exec --no-startup-id $HOME/.config/i3/todo.sh
    exec_always --no-startup-id i3-msg 'rename workspace "1" to "5:d|empty"' 
## end startups

# FONTS
    # Font for window titles.
    font pango:FontAwesome 12

# KEYBINDINGS
## platform dependent keybindings
    # set global modifier variable
    set $mod Mod1
    # lock screen
    bindsym XF86ScreenSaver exec $HOME/.config/i3/lock_screen.sh 
    # suspend
    bindsym XF86Sleep exec systemctl suspend  
    # screenshot
    bindsym Print exec deepin-screenshot

    #increase sound volume
    bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q sset Master 3%+ 
    #decrease sound volume
    bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q sset Master 3%- 
    #mute sound
    bindsym XF86AudioMute exec --no-startup-id amixer -q sset Master toggle 
## end platform dependent keybindings

## manipulate windows
    # drag floating windows
    floating_modifier $mod

    # kill focused window
    bindsym $mod+ctrl+c kill

    # change focus
    bindsym $mod+h focus left
    bindsym $mod+l focus right
    bindsym $mod+k focus up
    bindsym $mod+j focus down

    # move focused window
    bindsym $mod+Shift+h move left
    bindsym $mod+Shift+j move down
    bindsym $mod+Shift+k move up
    bindsym $mod+Shift+l move right

    # cursore keys
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # split in horizontal orientation
    bindsym $mod+w split h
    # split in vertical orientation
    bindsym $mod+v split v
    # enter fullscreen mode for the focused container
    bindsym $mod+f fullscreen toggle
    # toggle tiling / floating
    bindsym $mod+Shift+space floating toggle
    # change focus between tiling / floating windows
    bindsym $mod+space focus mode_toggle

    # kill window with mouse middle button
    bindsym --release button2 kill
## end manipulate windows

#i3 marks settings
    #show_marks yes
    #bindsym $mod+x exec i3-input -F 'mark %s' -l 1 -P 'Mark: '
    #bindsym $mod+y exec i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Goto: '
#end i3 marks settings
 
## launch or control applications
    # start a terminal
    bindsym $mod+s exec alacritty
    
    # control vim todo list
    bindsym $mod+Tab workspace $ws_todo; exec $HOME/.config/i3/todo.sh

    #start dmenu
    bindsym $mod+e exec "rofi -show combi"
## end launch or control applications

## manipulate workspaces
    # append and rename workspace
    bindsym $mod+a exec $HOME/.config/i3/i3wsorg.py append
    bindsym $mod+r exec $HOME/.config/i3/i3wsorg.py rename

    # switching between workspaces
    bindsym $mod+Shift+N workspace prev
    bindsym $mod+n workspace next

    # switching between workspaces
    bindsym $mod+ctrl+h focus output left
    bindsym $mod+ctrl+l focus output right
    bindsym $mod+ctrl+k focus output up
    bindsym $mod+ctrl+j focus output down

    # change container layout (stacked, tabbed, toggle split)
    bindsym $mod+x layout stacking
    bindsym $mod+b layout tabbed
    bindsym $mod+m layout toggle split
    
    # returning on the previous position with the same key
    workspace_auto_back_and_forth yes
## end manipulate workspaces

## WM defaults
    # reload the configuration file
    bindsym $mod+Shift+R reload

    # restart i3 inplace 
    bindsym $mod+Shift+P restart

    # exit i3 (logout)
    bindsym $mod+Shift+greater exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
## end WM defaults


# RULES
## window rules 
    no_focus [window_role="pop-up"]
    no_focus [window_role="*"]
    no_focus [class="Xfce4-notifyd" window_type="notification"]
    for_window [window_role="pop-up"] floating enable
    for_window [window_role="task-dialog"] floating enable
    for_window [class="(?i)Yad"] floating enable
    for_window [class="Xfce4-.*"] floating enable
    for_window [class="Xfce4-notifyd" window_type="notification"] border none
## end window rules

## assigns for different applications
    # workspace containing browser
    assign [class="(?i)firefox"] workspace $ws_browser
    workspace $ws_browser output primary

    # workspace containing mail client
    assign [class="(?i)Evolution"] workspace $ws_mail
    workspace $ws_mail output primary

    # workspace containing todo list
    assign [title="(?i)todo-list"] workspace $ws_todo
    workspace $ws_todo output primary

    # workspace music 
    for_window [class="(?i)Spotify"] move to workspace $ws_music
    workspace $ws_music output primary

    # workspace gajim
    assign [class="(?i)gajim"] workspace $ws_chat
    workspace $ws_chat output primary

## end assigns for different applications
